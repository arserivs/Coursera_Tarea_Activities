<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <style name="MyTheme" parent="android:style/Theme.NoTitleBar">
        <item name="android:editTextStyle">@style/CamposEditables</item>
        <item name="android:layout_marginTop">20dp</item>
    </style>

    <style name="CamposEditables" parent="android:style/Widget.EditText">
        <item name="android:colorActivatedHighlight">@color/colorCampoActivo</item>
        <item name="android:colorBackground">@color/colorCampoFondo</item>
        <item name="android:color">@color/colorCampoTexto</item>
        <item name="android:textSize">14dp</item>
        <item name="android:layout_marginTop">20dp</item>
    </style>
</resources>
